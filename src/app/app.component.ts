import { Component } from '@angular/core';


/**
 * A component that is responsible for showing the HomeComponent that resides the initial Home Screen UI for the page
 */
@Component({
  selector: 'app-root',
  template: `<app-home></app-home>`
})
export class AppComponent {
  
  constructor() {}
  
}
