/**
 * An interface that is responsible for the ListItem typings which any static data referenced to this interface will be group and iterated.
 */
export interface ListItem {
  type: string;
  label?: string;
  class?: string;
}
